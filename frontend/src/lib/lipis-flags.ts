/** 4×3 SVG flags from https://flagicons.lipis.dev. */
export function lipisFlag4x3Url(countryCode: string): string {
  return `https://flagicons.lipis.dev/flags/4x3/${countryCode.toLowerCase()}.svg`;
}
